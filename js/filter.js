// filter.js — example extra filters (can extend: location, price-range)
// currently left minimal: you can add more UI and call window.app.applyFilters(...)
(function(){
  // placeholder for future filter integrations (e.g., location dropdown)
  // Example usage:
  // window.app.applyFilters({ keyword: '', category: 'tradisional', minRating: '4.5', location: '' })
})();
